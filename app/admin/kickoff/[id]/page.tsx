import{notFound,redirect}from"next/navigation";import{KickoffWorkspace}from"@/components/KickoffWorkspace";import{getAdminSession}from"@/lib/admin-auth";import{getKickoff}from"@/lib/kickoffs";export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{id:string}>}){if(!(await getAdminSession()))redirect("/admin/login");const kickoff=getKickoff((await params).id);if(!kickoff)notFound();return <KickoffWorkspace initial={kickoff}/>}
