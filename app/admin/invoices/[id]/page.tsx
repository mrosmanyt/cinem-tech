import { notFound, redirect } from "next/navigation"; import { InvoiceEditor } from "@/components/InvoiceEditor"; import { getAdminSession } from "@/lib/admin-auth"; import { getInvoice } from "@/lib/invoices"; export const dynamic = "force-dynamic"; export default async function Page({ params }: { params: Promise<{ id: string }> }) { if (!(await getAdminSession())) redirect("/admin/login"); const invoice = getInvoice((await params).id); if (!invoice) notFound(); return <InvoiceEditor initial={invoice} />; }
